"""
[파일] Camera_v4_high_speed.py
[목적] Camera_v4 실전 고속 주행 버전 (기존 대비 속도 1.5배 상향 및 제어 밸런싱)
[개선] 고속 주행에 따른 오버슈트 방지를 위해 조향 게인 및 필터 계수 최적화
"""

import os
import atexit
import signal
import sys
import math
import threading
import serial
import time
import cv2
import numpy as np

from color_v2 import (load_calibration,
                       get_red_mask, get_yellow_mask, get_blue_mask,
                       RED_LOWER1, RED_UPPER1, RED_LOWER2, RED_UPPER2,
                       YELLOW_LOWER, YELLOW_UPPER,
                       BLUE_LOWER, BLUE_UPPER)


# ─────────────────────────────────────────────────────────────────────────────
#  하드웨어 설정
# ─────────────────────────────────────────────────────────────────────────────
PORT_ARDU  = "/dev/ttyS0"
PORT_LIDAR = "/dev/ttyUSB0"
CAM_INDEX  = 0
CAM_W, CAM_H = 640, 480
CALIB_FILE = os.path.join(os.path.dirname(os.path.abspath(__file__)),
                           "camera_calibration.pkl")

# ─────────────────────────────────────────────────────────────────────────────
#  탐지 파라미터
# ─────────────────────────────────────────────────────────────────────────────
MIN_AREA       = 1000   # 강탐지 최소 컨투어 면적 (640×480 기준 px²)
WEAK_MIN_AREA  = 200    # 약탐지 최소 컨투어 면적
AR_MIN         = 0.5    # minAreaRect 종횡비 정상 범위 하한
AR_MAX         = 2.0    # minAreaRect 종횡비 정상 범위 상한

# ─────────────────────────────────────────────────────────────────────────────
#  카메라 주행 및 속도 1.5배 상향 튜닝 파라미터
# ─────────────────────────────────────────────────────────────────────────────
MAX_STEER         = 1.0
SPEED_FAR         = 0.83   # [기존 0.55] -> 1.5배 상향
SPEED_NEAR        = 0.53   # [기존 0.35] -> 1.5배 상향
PIVOT_SPEED       = 0.30   # [기존 0.20] -> 제자리 회전 속도 보강
WEAK_SPEED        = 0.53   # [기존 0.35] -> 1.5배 상향
NUDGE_SPEED       = 0.38   # [기존 0.25] -> 1.5배 상향 (저속 전진 구간)

# 고속 주행 대응 제어 파라미터 밸런싱
WEAK_STEER_GAIN   = 0.80   # [기존 0.60] -> 고속 이탈 방지를 위한 약탐지 조향 보강
STEER_SMOOTH_ALPHA = 0.55  # [기존 0.45] -> 속도가 빨라진 만큼 반응성을 높이기 위해 EMA 가중치 증가

AREA_SLOW_THRES   = 0.20   
AREA_PEAK_THRES   = 0.20   
CONFIRM_FRAMES    = 8      # [기존 10] -> 고속 주행 시 10프레임 대기는 너무 길어 8프레임으로 단축
STOP_DURATION     = 1.1    
COLOR_MEMORY_TIME = 0.6   

WALL_ENTER_FRAMES = 4     # [기존 5] -> 고속 이동 시 벽 타는 타이밍을 빠르게 잡기 위해 4프레임으로 단축
REGAIN_CONTOUR_THRES = 500 

TARGETS = ['red', 'yellow', 'blue']

# ─────────────────────────────────────────────────────────────────────────────
#  LiDAR VFH 파라미터 (고속 주행 감속 트리거 상향)
# ─────────────────────────────────────────────────────────────────────────────
BIN_DEG       = 4.0
N_BINS        = int(360 / BIN_DEG)
GAP_MIN_PASS  = 100.0
DETECT        = 950.0      # [기존 800.0] -> 속도가 빨라졌으므로 더 먼 거리에서 장애물 인지 시작
VELO_DOWN     = 550.0      # [기존 400.0] -> 감속 시작 트리거 거리 상향
EMERGENCY     = 250.0      # [기존 200.0] -> 고속 제동 거리를 감안하여 비상정지 마진 확대
LID_MAX_STEER = 1.2
ROT_THRESH    = 100.0
ROBOT_RADIUS  = 60.0

OBS_RETURN_TIME = 2.0   
OBS_RETURN_GAIN = 0.70   

# ─────────────────────────────────────────────────────────────────────────────
#  LiDAR 공유 상태
# ─────────────────────────────────────────────────────────────────────────────
_lidar_lock  = threading.Lock()
_lidar_state = {
    'has_data'   : False,
    'emg_near'   : 9999.0,
    'front_near' : 9999.0,
    'vfh_action' : 'FWD',
    'vfh_steer'  : 0.0,
    'vfh_speed'  : 0.85,  # VFH 기본 속도도 상향 반영
    'rot_dir'    : 1.0,
    'lat_L'      : 9999.0,
    'lat_R'      : 9999.0,
    'nearest_d'  : 9999.0,
    'nearest_a'  : 0.0,
}


# ─────────────────────────────────────────────────────────────────────────────
#  LiDAR VFH 헬퍼
# ─────────────────────────────────────────────────────────────────────────────
def _build_hist(scan_buf):
    hist   = [9999.0] * N_BINS
    has_pt = [False]  * N_BINS
    for a, d in scan_buf:
        idx = int(a / BIN_DEG) % N_BINS
        if d < hist[idx]:
            hist[idx] = d
            has_pt[idx] = True
    return hist, has_pt


def _nearest(hist, has_pt, center_cw, arc_half=25):
    cb = int(center_cw / BIN_DEG) % N_BINS
    nc = max(1, int(arc_half / BIN_DEG))
    md = 9999.0
    for k in range(-nc, nc + 1):
        idx = (cb + k) % N_BINS
        if has_pt[idx] and hist[idx] < md:
            md = hist[idx]
    return md


def _find_gaps(hist, has_pt):
    blocked = [has_pt[i] and hist[i] <= DETECT for i in range(N_BINS)]
    smoothed = blocked[:]
    for i in range(N_BINS):
        if blocked[i] and not blocked[(i-1) % N_BINS] and not blocked[(i+1) % N_BINS]:
            smoothed[i] = False
    blocked = smoothed
    inflated = blocked[:]
    for i in range(N_BINS):
        if blocked[i] and hist[i] < 9999.0:
            ar  = math.asin(min(1.0, ROBOT_RADIUS / max(hist[i], ROBOT_RADIUS)))
            ab  = int(math.degrees(ar) / BIN_DEG) + 1
            for k in range(-ab, ab + 1):
                inflated[(i + k) % N_BINS] = True
    blocked = inflated
    gaps = []; seen = set(); i = 0
    while i < 2 * N_BINS:
        if not blocked[i % N_BINS]:
            j = i + 1
            while j < i + N_BINS and not blocked[j % N_BINS]:
                j += 1
            span = j - i
            if span < N_BINS:
                ccw = ((i + j) / 2.0 * BIN_DEG) % 360.0
                ck  = round(ccw)
                if ck not in seen:
                    seen.add(ck)
                    dg = span * BIN_DEG
                    dL = min(hist[(i-1) % N_BINS] if has_pt[(i-1) % N_BINS] else DETECT, DETECT)
                    dR = min(hist[j % N_BINS]     if has_pt[j % N_BINS]     else DETECT, DETECT)
                    gw = (dL + dR) * math.sin(math.radians(dg / 2.0))
                    dp = min(hist[k % N_BINS] for k in range(i, j))
                    cs = ccw if ccw <= 180.0 else ccw - 360.0
                    gaps.append({'center': cs, 'center_cw': ccw, 'width': gw,
                                 'passable': gw >= GAP_MIN_PASS, 'delta_deg': dg,
                                 'd_L': dL, 'd_R': dR, 'depth': dp})
            i = j
        else:
            i += 1
    return gaps


def _best_gap(gaps):
    if not gaps:
        return None
    pool = [g for g in gaps if g['passable']] or gaps
    return max(pool, key=lambda g: g['width'] * 0.3
                                   - abs(g['center']) * 1.6
                                   + min(g['depth'], DETECT) / DETECT * 25.0)


def _compute_vfh(hist, has_pt):
    emg   = _nearest(hist, has_pt, 0.0,   arc_half=80)
    front = _nearest(hist, has_pt, 0.0,   arc_half=35)
    lL    = _nearest(hist, has_pt, 270.0, arc_half=45)
    lR    = _nearest(hist, has_pt,  90.0, arc_half=45)
    if not any(has_pt):
        return 'FWD', 0.0, 0.75, 1.0, emg, front, lL, lR  # 고속 매핑
    gaps = _find_gaps(hist, has_pt)
    best = _best_gap(gaps)
    if best is not None and best['passable'] and abs(best['center']) <= ROT_THRESH:
        imb  = (best['d_R'] - best['d_L']) / (best['d_L'] + best['d_R'] + 1e-9)
        bias = imb * (best['delta_deg'] / 2.9)
        WR   = 150.0
        rep  = (max(0.0, WR - lL) / WR - max(0.0, WR - lR) / WR) * 20.0
        CR   = 350.0
        cL   = _nearest(hist, has_pt, 320.0, arc_half=25)
        cR   = _nearest(hist, has_pt,  40.0, arc_half=25)
        crn  = (max(0.0, CR - cL) / CR - max(0.0, CR - cR) / CR) * 45.0
        tgt  = best['center'] + bias + rep + crn
        nd   = _nearest(hist, has_pt, best['center_cw'], arc_half=35)
        rt   = min(max((VELO_DOWN - nd) / (VELO_DOWN - EMERGENCY), 0.0), 1.0)
        st   = max(-LID_MAX_STEER, min(LID_MAX_STEER,
               tgt * (1.0 + rt * 0.5) / 90.0 * LID_MAX_STEER))
        spd  = 0.85 * (1.0 - rt * 0.55)  # 고속 주행 보강 계산
        return 'FWD', float(st), float(spd), 1.0, emg, front, lL, lR
    FARC = 60.0
    if gaps:
        fg = [g for g in gaps if abs(g['center']) <= FARC]
        td = max(fg, key=lambda g: g['width'])['center'] if fg \
             else max(-FARC, min(FARC, max(gaps, key=lambda g: g['width'])['center']))
    else:
        td = 0.0
    st = max(-LID_MAX_STEER, min(LID_MAX_STEER, td / 90.0 * LID_MAX_STEER * 0.5))
    return 'FWD', float(st), 0.55, 1.0, emg, front, lL, lR


# ─────────────────────────────────────────────────────────────────────────────
#  LiDAR 백그라운드 스레드 및 주행 제어 함수
# ─────────────────────────────────────────────────────────────────────────────
def _lidar_worker(ser_l):
    scan_buf = []
    while True:
        try:
            data = ser_l.read(5)
            if len(data) != 5:
                continue
            s_flag     = data[0] & 0x01
            s_inv_flag = (data[0] & 0x02) >> 1
            if s_inv_flag != (1 - s_flag):
                continue
            if (data[1] & 0x01) != 1:
                continue
            quality  = data[0] >> 2
            angle    = ((data[1] >> 1) | (data[2] << 7)) / 64.0
            distance = (data[3] | (data[4] << 8)) / 4.0
            if quality == 0 or distance < 80:
                continue
            scan_buf.append((angle, distance))
            if s_flag == 1 and scan_buf:
                nearest_d = 9999.0
                nearest_a = 0.0
                for a, d in scan_buf:
                    if 80 < d < nearest_d:
                        nearest_d = d
                        nearest_a = a

                hist, has_pt = _build_hist(scan_buf)
                act, st, spd, rd, emg, front, lL, lR = _compute_vfh(hist, has_pt)
                with _lidar_lock:
                    _lidar_state['has_data']   = True
                    _lidar_state['emg_near']   = emg
                    _lidar_state['front_near'] = front
                    _lidar_state['vfh_action'] = act
                    _lidar_state['vfh_steer']  = st
                    _lidar_state['vfh_speed']  = spd
                    _lidar_state['rot_dir']    = rd
                    _lidar_state['lat_L']      = lL
                    _lidar_state['lat_R']      = lR
                    _lidar_state['nearest_d'] = nearest_d
                    _lidar_state['nearest_a'] = nearest_a
                scan_buf = []
        except Exception as e:
            print(f"[LIDAR] {e}")
            scan_buf = []


def _lidar_read():
    with _lidar_lock:
        return dict(_lidar_state)


def _obstacle_follow_drive(ser, color_dir):
    ls = _lidar_read()
    target_dist = 450.0

    if color_dir < 0:
        wall_dist = ls['lat_L']
        err = target_dist - wall_dist
        side_bias = (ls['lat_L'] - ls['lat_R']) * 0.0015
        steer = np.clip(-err * 0.0035 + side_bias, -0.7, 0.7) # 고속 추종 조향각 살짝 상향
    else:
        wall_dist = ls['lat_R']
        err = target_dist - wall_dist
        steer = np.clip(err * 0.0035, -0.7, 0.7)

    speed = _speed_limit(0.70) # [기존 0.50] -> 월 팔로잉 속도 상향
    if speed <= 0:
        return "STOP"
    else:
        cmd_steer = float(np.clip(steer, -MAX_STEER, MAX_STEER))
        cmd_speed = float(speed)
        ser.write(f"F {cmd_steer:.2f} {cmd_speed:.2f}\n".encode())

    return f"WALL d={wall_dist:.0f} err={err:.0f} st={steer:+.2f}"


def _vfh_drive(ser):
    ls = _lidar_read()
    if not ls['has_data']:
        ser.write(b"S\n")
        return "NO_LIDAR"

    steer = ls['vfh_steer']
    speed = ls['vfh_speed']
    safe_speed = _speed_limit(speed)

    if safe_speed <= 0:
        ser.write(b"S\n")
        return "VFH_STOP"

    cmd_steer = float(np.clip(steer, -MAX_STEER, MAX_STEER))
    cmd_speed = float(safe_speed)
    ser.write(f"F {cmd_steer:.2f} {cmd_speed:.2f}\n".encode())
    return f"VFH st={cmd_steer:+.2f} spd={cmd_speed:.2f}"


def _speed_limit(cam_speed: float) -> float:
    ls = _lidar_read()
    if not ls['has_data']:
        return cam_speed
    front = ls['front_near']
    if front <= EMERGENCY:
        return 0.0
    if front < VELO_DOWN:
        rt = max(0.0, min(1.0, (VELO_DOWN - front) / (VELO_DOWN - EMERGENCY)))
        return cam_speed * (1.0 - rt * 0.35) # 고속인 만큼 장애물 인지 시 감속폭 확대
    return cam_speed


# ─────────────────────────────────────────────────────────────────────────────
#  경량 색지 탐지 및 중심 추출
# ─────────────────────────────────────────────────────────────────────────────
def _get_mask(hsv, color: str):
    if color == 'red':    return get_red_mask(hsv)
    if color == 'yellow': return get_yellow_mask(hsv)
    return get_blue_mask(hsv)


def _detect_paper(hsv: np.ndarray, color: str):
    mask = _get_mask(hsv, color)
    cnts, _ = cv2.findContours(mask, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)
    scored = [(c, cv2.contourArea(c)) for c in cnts]
    scored = [(c, a) for c, a in scored if a > MIN_AREA]
    if not scored:
        return None

    cnt, area = max(scored, key=lambda x: x[1])
    area_r = area / (CAM_W * CAM_H)

    rect = cv2.minAreaRect(cnt)
    (cx, cy), (w, h), angle = rect
    
    if w > 0 and h > 0:
        aspect_ratio = max(w, h) / min(w, h)
        if AR_MIN <= aspect_ratio <= AR_MAX:
            return {'cx': cx, 'cy': cy, 'area_r': area_r, 'contour': cnt}

    try:
        roi_mask = np.zeros(mask.shape, dtype=np.uint8)
        cv2.drawContours(roi_mask, [cnt], -1, 255, -1)
        dist_transform = cv2.distanceTransform(roi_mask, cv2.DIST_L2, 5)
        _, _, _, max_loc = cv2.minMaxLoc(dist_transform)
        cx, cy = max_loc[0], max_loc[1]
    except Exception:
        M = cv2.moments(cnt)
        if M['m00'] != 0:
            cx = M['m10'] / M['m00']
            cy = M['m01'] / M['m00']
        else:
            return None

    return {
        'cx': cx, 'cy': cy,
        'area_r': area_r,
        'contour': cnt,
    }


def _weak_detect(hsv: np.ndarray, color: str):
    if color == 'red':
        mask = cv2.bitwise_or(cv2.inRange(hsv, RED_LOWER1, RED_UPPER1),
                              cv2.inRange(hsv, RED_LOWER2, RED_UPPER2))
    elif color == 'yellow':
        mask = cv2.inRange(hsv, YELLOW_LOWER, YELLOW_UPPER)
    else:
        mask = cv2.inRange(hsv, BLUE_LOWER, BLUE_UPPER)
        
    cnts, _ = cv2.findContours(mask, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)
    scored = [(c, cv2.contourArea(c)) for c in cnts]
    scored = [(c, a) for c, a in scored if a > WEAK_MIN_AREA]
    if not scored:
        return None
    cnt, _ = max(scored, key=lambda x: x[1])
    M = cv2.moments(cnt)
    if M['m00'] == 0:
        return None
    return cnt, M['m10'] / M['m00'], M['m01'] / M['m00']


def _offset(cx: float) -> float:
    return (cx - CAM_W / 2) / (CAM_W / 2)


def _draw_ctr(vis, cx, cy, color):
    x, y = int(cx), int(cy)
    cv2.circle(vis, (x, y), 5, color, -1)
    cv2.line(vis, (x - 12, y), (x + 12, y), color, 1)
    cv2.line(vis, (x, y - 12), (x, y + 12), color, 1)


# ─────────────────────────────────────────────────────────────────────────────
#  메인 루프 실행부
# ─────────────────────────────────────────────────────────────────────────────
def main():
    ser   = serial.Serial(PORT_ARDU,  460800, timeout=1)
    ser_l = serial.Serial(PORT_LIDAR, 460800, timeout=1)

    ser_l.write(bytes([0xA5, 0x40]))
    time.sleep(1.0)
    ser_l.reset_input_buffer()
    ser_l.write(bytes([0xA5, 0x20]))
    print(f"[LIDAR] 스캔 시작: {PORT_LIDAR}")

    t_lidar = threading.Thread(target=_lidar_worker, args=(ser_l,), daemon=True)
    t_lidar.start()

    cap = cv2.VideoCapture(CAM_INDEX)
    cap.set(cv2.CAP_PROP_FRAME_WIDTH,  CAM_W)
    cap.set(cv2.CAP_PROP_FRAME_HEIGHT, CAM_H)
    cap.set(cv2.CAP_PROP_FPS, 30)
    cap.set(cv2.CAP_PROP_BUFFERSIZE, 1)

    cam_mat, dist_coeffs, calib_res = load_calibration(CALIB_FILE)
    map1 = map2 = None
    if cam_mat is not None and calib_res == (CAM_W, CAM_H):
        new_mtx, _ = cv2.getOptimalNewCameraMatrix(
            cam_mat, dist_coeffs, (CAM_W, CAM_H), 1, (CAM_W, CAM_H))
        map1, map2 = cv2.initUndistortRectifyMap(
            cam_mat, dist_coeffs, None, new_mtx, (CAM_W, CAM_H), cv2.CV_16SC2)
        print("[CAM] 캘리브레이션 적용")
    else:
        print("[CAM] 캘리브레이션 없음 (raw 사용)")

    def _cleanup():
        try:
            ser.write(b"S\n"); time.sleep(0.1)
            ser_l.write(bytes([0xA5, 0x25])); time.sleep(0.1)
            cap.release(); cv2.destroyAllWindows()
            ser.close(); ser_l.close()
        except Exception:
            pass
    atexit.register(_cleanup)

    def _sig(_s, _f):
        _cleanup(); sys.exit(0)
    signal.signal(signal.SIGINT,  _sig)
    signal.signal(signal.SIGTERM, _sig)
    signal.signal(signal.SIGTSTP, _sig)

    # 상태 제어 변수
    target_idx     = 0
    state          = 'SEEK'  
    on_zone_count  = 0
    stop_start     = None
    last_seen      = time.time()
    smoothed_steer = 0.0
    area_peak_seen        = False
    peak_area_r           = 0.0
    approach_steer_locked = False
    locked_approach_steer = 0.0
    prev_area_r           = 0.0
    
    mem_invis_count       = 0  
    last_seen_color_offset = 0.0  

    print("=" * 60)
    print("  Camera_v4_HighSpeed | 속도 1.5배 업그레이드 레이싱 버전")
    print(f"  처리해상도: {CAM_W}×{CAM_H}   목표: RED→YELLOW→BLUE")
    print("=" * 60)

    while True:
        ls = _lidar_read()

        if ls['emg_near'] < EMERGENCY:
            ser.write(b"S\n")
            state = 'SEEK'
            continue
            
        ret, raw = cap.read()
        if not ret:
            time.sleep(0.01)
            continue

        undist = cv2.remap(raw, map1, map2, cv2.INTER_LINEAR) if map1 is not None else raw
        vis    = undist.copy()
        hsv    = cv2.cvtColor(undist, cv2.COLOR_BGR2HSV)

        if state == 'DONE':
            ser.write(b"S\n")
            cv2.putText(vis, "MISSION COMPLETE", (40, CAM_H // 2),
                        cv2.FONT_HERSHEY_SIMPLEX, 1.0, (0, 255, 0), 2)
            cv2.imshow('Robot View', vis)
            cv2.waitKey(1); time.sleep(0.1)
            continue

        color = TARGETS[target_idx]

        if state == 'STOP':
            ser.write(b"S\n")
            elapsed = time.time() - stop_start
            remain  = max(0.0, STOP_DURATION - elapsed)
            cv2.putText(vis, f"STOP {color.upper()}  {remain:.1f}s",
                        (CAM_W // 2 - 140, 56), cv2.FONT_HERSHEY_SIMPLEX,
                        0.85, (0, 255, 255), 2)
            cv2.imshow('Robot View', vis)
            cv2.waitKey(1)
            if elapsed >= STOP_DURATION:
                if target_idx < len(TARGETS) - 1:
                    target_idx            += 1
                    state                  = 'SEEK'
                    on_zone_count          = 0
                    area_peak_seen         = False
                    peak_area_r            = 0.0
                    approach_steer_locked  = False
                    locked_approach_steer  = 0.0
                    prev_area_r            = 0.0
                    mem_invis_count        = 0
                    last_seen              = time.time()
                    print(f"  ✅ {color.upper()} 완료 → {TARGETS[target_idx].upper()}")
                else:
                    state = 'DONE'
                    print("  ✅ 전체 미션 완료!")
            continue

        det = _detect_paper(hsv, color)
        weak = _weak_detect(hsv, color)

        # ① 강탐지 추적 모드 (SEEK) ───────────────────────────────────────────
        if det is not None:
            mem_invis_count = 0  
            last_seen = time.time()
            cx, cy    = det['cx'], det['cy']
            area_r    = det['area_r']
            offset    = _offset(cx)
            last_seen_color_offset = offset  
            dcx, dcy  = int(cx), int(cy)

            if area_r >= AREA_PEAK_THRES:
                area_peak_seen = True
                peak_area_r    = max(peak_area_r, area_r)

                if not approach_steer_locked:
                    if prev_area_r > 0.05:
                        # 고속 진입 시 조향 락 게인을 약간 강하게 조율 (0.25 -> 0.30)
                        locked_approach_steer = float(np.clip(offset * 0.30, -0.40, 0.40))
                    else:
                        locked_approach_steer = 0.0
                    approach_steer_locked = True
                steer   = locked_approach_steer
                speed   = _speed_limit(SPEED_NEAR)
                src     = "approach" if prev_area_r > 0.05 else "sudden"
                log_msg = f"CLOSE-FWD(lock/{src}) st={steer:+.2f} area={area_r:.2f}"
                cv2.putText(vis, f"CLOSE-FWD lock={steer:+.2f}", (CAM_W // 2 - 180, 44), 
                            cv2.FONT_HERSHEY_SIMPLEX, 0.65, (0, 255, 180), 2)
            else:
                approach_steer_locked = False
                # 고속 횡이동 조향 타겟 게인 미세 밸런싱 (1.5 -> 1.3, 오버슈트 억제)
                steer = float(np.clip(offset * 1.3, -MAX_STEER, MAX_STEER))
                if abs(steer) < 0.4:
                    speed   = _speed_limit(SPEED_FAR)  # 원거리 직진 고속 가속
                    log_msg = f"FWD-FAST off={offset:+.2f}"
                else:
                    speed   = PIVOT_SPEED
                    arrow   = '→' if steer > 0 else '←'
                    log_msg = f"CURVE{arrow} off={offset:+.2f}"

            x, y, w, h = cv2.boundingRect(det['contour'])
            cv2.rectangle(vis, (x, y), (x + w, y + h), (0, 180, 255), 2)
            _draw_ctr(vis, dcx, dcy, (0, 180, 255))

            smoothed_steer = STEER_SMOOTH_ALPHA * steer + (1.0 - STEER_SMOOTH_ALPHA) * smoothed_steer
            prev_area_r    = area_r

            if speed > 0:
                print(f"  [SEEK] {color.upper()} {log_msg} spd={speed:.2f}")
                cmd_steer = float(np.clip(steer, -MAX_STEER, MAX_STEER))
                ser.write(f"F {cmd_steer:.2f} {speed:.2f}\n".encode())
            else:
                vfh_log = _vfh_drive(ser)
                print(f"  [SEEK→VFH] {color.upper()} obstacle→ {vfh_log}")

        # ② 피크 진입 완료 후 타겟 하단 가림 현상 대응 (ENTERING 상태) ─────────────
        elif area_peak_seen:
            prev_area_r = 0.0
            if weak is not None:
                mem_invis_count = 0
                cnt_w, wcx, wcy = weak
                weak_offset    = _offset(wcx)
                steer          = float(np.clip(weak_offset * WEAK_STEER_GAIN, -MAX_STEER, MAX_STEER))
                last_seen      = time.time()
                smoothed_steer = STEER_SMOOTH_ALPHA * steer + (1.0 - STEER_SMOOTH_ALPHA) * smoothed_steer
                
                cv2.drawContours(vis, [cnt_w], -1, (180, 255, 0), 1)
                _draw_ctr(vis, int(wcx), int(wcy), (180, 255, 0))
                cv2.putText(vis, f"ENTERING {color.upper()} off={weak_offset:+.2f}",
                            (10, 80), cv2.FONT_HERSHEY_SIMPLEX, 0.65, (180, 255, 0), 2)
                
                weak_speed = _speed_limit(WEAK_SPEED)
                if weak_speed <= 0:
                    ser.write(b"S\n")
                else:
                    ser.write(f"F {smoothed_steer:.2f} {weak_speed:.2f}\n".encode())
            else:
                on_zone_count += 1
                nudge_spd = _speed_limit(NUDGE_SPEED)
                cv2.putText(vis, f"INVISIBLE cnt:{on_zone_count}/{CONFIRM_FRAMES}",
                            (10, 80), cv2.FONT_HERSHEY_SIMPLEX, 0.65, (0, 255, 180), 2)
                
                if on_zone_count >= CONFIRM_FRAMES:
                    state = 'STOP'
                    stop_start = time.time()
                    ser.write(b"S\n")
                    print(f"  🎯 {color.upper()} 도달! peak={peak_area_r:.2f}")
                    continue
                else:
                    ser.write(f"F {smoothed_steer:.2f} {nudge_spd:.2f}\n".encode())

        # ③ 타겟 완전 미탐지 및 센서 필터링 마진 제어 분기 ────────────────────────
        else:
            prev_area_r   = 0.0
            on_zone_count = max(0, on_zone_count - 1)
            mem_invis_count += 1  

            is_valid_regain = False
            if weak is not None:
                cnt_w, wcx, wcy = weak
                if cv2.contourArea(cnt_w) > REGAIN_CONTOUR_THRES: 
                    is_valid_regain = True

            if is_valid_regain:
                mem_invis_count = 0 
                cnt_w, wcx, wcy = weak
                weak_offset = _offset(wcx)
                # 속도가 빨라 복귀할 때 강한 피드백이 필요하므로 게인을 상향 (2.5 -> 2.8)
                w_steer = float(np.clip(weak_offset * 2.8, -MAX_STEER, MAX_STEER))
                last_seen = time.time()
                last_seen_color_offset = weak_offset

                cv2.drawContours(vis, [cnt_w], -1, (180, 180, 0), 1)
                cv2.putText(vis, f"COLOR-REGAIN off={weak_offset:+.2f}",
                            (10, 80), cv2.FONT_HERSHEY_SIMPLEX, 0.65, (180, 180, 0), 2)

                final_speed = _speed_limit(WEAK_SPEED)
                ser.write(f"F {w_steer:.2f} {final_speed:.2f}\n".encode())
            else:
                color_dir = 1.0 if last_seen_color_offset >= 0 else -1.0

                if mem_invis_count < WALL_ENTER_FRAMES: 
                    nudge_spd = _speed_limit(NUDGE_SPEED)
                    ser.write(f"F {smoothed_steer:.2f} {nudge_spd:.2f}\n".encode())
                    cv2.putText(vis, f"LOSS HOLD {mem_invis_count}", (10, 80),
                                cv2.FONT_HERSHEY_SIMPLEX, 0.60, (100, 100, 255), 2)
                else:
                    log = _obstacle_follow_drive(ser, color_dir)
                    cv2.putText(vis, f"WALL-FOLLOW {log}", (10, 80),
                                cv2.FONT_HERSHEY_SIMPLEX, 0.60, (0, 255, 255), 2)

                if mem_invis_count >= CONFIRM_FRAMES:
                    state = 'STOP'
                    stop_start = time.time()
                    ser.write(b"S\n")
                    print(f"  🎯 {color.upper()} 도달 (wall-follow 종료)")
                    continue

        # ── 상단 공통 비주얼 피드백 HUD 출력 ─────────────────────────────────
        emg_txt = f" EMG:{ls['emg_near']:.0f}mm" if ls['has_data'] else ""
        cv2.putText(vis, f"{state}|{color.upper()}|invis:{mem_invis_count} {emg_txt}",
                    (10, CAM_H - 16), cv2.FONT_HERSHEY_SIMPLEX, 0.55, (255, 255, 0), 2)

        cv2.imshow('Robot View', vis)
        if (cv2.waitKey(1) & 0xFF) == ord('q'):
            break

    ser.write(b"S\n")
    cap.release()
    cv2.destroyAllWindows()


if __name__ == '__main__':
    main()
